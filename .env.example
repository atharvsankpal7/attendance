VITE_API_BASE=http://localhost:4000/api

# MongoDB connection (server)
MONGO_URI=mongodb://127.0.0.1:27017/attendance_local

# Notes:
# - Frontend expects VITE_API_BASE to point at the running backend API.
# - Run the server with `cd server && npm install && npm run dev` and ensure MongoDB is running locally.